DEBUG="true"
SECRET_KEY="none"

# Use local sqlite db
DB_ENGINE="django.db.backends.sqlite3"
DB_NAME="db.sqlite3"

# Log emails to console instead of actually sending
EMAIL_BACKEND="django.core.mail.backends.console.EmailBackend"
